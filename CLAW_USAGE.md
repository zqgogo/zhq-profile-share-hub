# Claw 使用口令文档

这份文档专门汇总目前 `profile-share-assistant` 这套能力，在 Claw / 微信里比较容易命中的说法。

适用项目：

`.`

对应 skill：

`/path/to/codex/skills/profile-share-assistant`

---

## 1. 使用前提

要让 Claw 正常调用这套能力，默认前提是：

- 本地项目服务已经启动
- Claw 能命中 `profile-share-assistant` 这个 skill

当前固定入口：

- [https://profile-share.zqgogo.workers.dev](https://profile-share.zqgogo.workers.dev)

---

## 2. 最推荐的说法

下面这些是最推荐直接在 Claw / 微信里说的话。

### 2.0 最稳的命中标识

建议以后统一用三层结构：

- `skill`
- `简历项目`
- `具体动作`

固定格式就是：

- `skill 简历项目 帮我生成一个简历页，信息如下：...`
- `skill 简历项目 帮我生成一个相亲交友资料页，信息如下：...`
- `skill 简历项目 根据这个管理链接把页面下线：...`
- `skill 简历项目 根据这个管理链接重置次数：...`
- `skill 简历项目 恢复分享链接`
- `skill 简历项目 重新恢复固定入口`

### 2.1 创建简历页

- `skill 简历项目 帮我生成一个简历页，信息如下：...`
- `帮我生成一个简历页，信息如下：...`
- `把这份简历做成分享链接：...`
- `根据这些内容生成一个 iOS 工程师简历页：...`

### 2.2 创建相亲交友页

- `skill 简历项目 帮我生成一个相亲交友资料页，信息如下：...`
- `帮我生成一个相亲交友资料页，信息如下：...`
- `把这些资料做成相亲交友分享页：...`
- `生成一个相亲交友链接，内容如下：...`

### 2.3 用管理链接管理页面

- `skill 简历项目 根据这个管理链接把页面下线：管理链接`
- `skill 简历项目 根据这个管理链接把页面重新上线：管理链接`
- `skill 简历项目 根据这个管理链接重置聊天和匹配次数：管理链接`
- `skill 简历项目 根据这个管理链接重新生成资料，新的内容是：...`
- `根据这个管理链接把页面下线：管理链接`
- `根据这个管理链接把页面重新上线：管理链接`
- `根据这个管理链接重置聊天和匹配次数：管理链接`
- `根据这个管理链接重新生成资料，新的内容是：...`

### 2.4 恢复固定入口

- `skill 简历项目 恢复分享链接`
- `skill 简历项目 重新恢复固定入口`
- `skill 简历项目 让所有页面重新可以访问`
- `恢复分享链接`
- `重新恢复固定入口`
- `让所有页面重新可以访问`

这组说法对应的是：

- 重建新的临时 tunnel
- 更新固定的 `workers.dev` 入口
- 让旧的固定入口链接重新恢复可访问

---

## 3. 当前支持的能力

### 3.1 创建类

- 创建简历页
- 创建相亲交友页
- 返回公开链接
- 返回管理链接

### 3.2 管理类

- 下线页面
- 重新上线页面
- 重置匹配次数
- 重置聊天次数
- 重新生成页面内容

### 3.3 恢复类

- 恢复固定入口
- 重新连通所有公开链接

注意：

- 这里恢复的是 `固定入口`
- 不是恢复旧的 `trycloudflare` 原始临时链接

---

## 4. 最稳的管理方式

如果你要让 Claw 准确识别“是哪一页”，最稳的方式是直接发：

- `管理链接`

推荐说法：

- `根据这个管理链接把页面下线：...`
- `根据这个管理链接重置次数：...`
- `根据这个管理链接重新生成：...`

原因：

- 管理链接自带页面 `id`
- 同时自带管理 `token`
- 不容易歧义

---

## 5. 不同场景推荐口令

### 5.1 求职 / 招聘

- `帮我生成一个简历页，信息如下：...`
- `把这份 iOS 简历做成分享链接：...`
- `根据这个管理链接重置次数：...`

### 5.2 相亲交友

- `帮我生成一个相亲交友资料页，信息如下：...`
- `根据这个管理链接重新生成资料，新的内容是：...`
- `根据这个管理链接把页面下线：...`

### 5.3 链接失效

- `恢复分享链接`
- `重新恢复固定入口`
- `让所有页面重新可以访问`

---

## 6. 返回结果通常是什么

### 6.1 创建页面时

一般会返回：

- 公开链接
- 管理链接

### 6.2 管理页面时

一般会返回：

- 操作结果
- 页面状态
- 剩余匹配次数
- 剩余聊天次数

### 6.3 恢复固定入口时

一般会返回：

- `fixed_url`
- `upstream_url`

其中：

- `fixed_url` 是你继续发给别人的固定入口
- `upstream_url` 是当前临时 tunnel 地址，只用于内部恢复确认

---

## 7. 目前不建议的说法

下面这些说法不够稳，容易歧义：

- `把那个页面恢复一下`
- `把我刚才那个链接修好`
- `把上次那个简历重新弄一下`

原因：

- 页面可能不止一份
- 链接可能有多个
- 仅靠标题或“上次那个”不够唯一

更好的说法是：

- 直接发 `管理链接`
- 或者明确说 `skill 简历项目 恢复分享链接`

---

## 8. 当前 skill 触发点汇总

你现在可以把它理解成三类命令：

### 8.1 创建

- `skill 简历项目 帮我生成一个简历页`
- `skill 简历项目 帮我生成一个相亲交友资料页`
- `skill 简历项目 把这些信息做成分享链接`
- `帮我生成一个简历页`
- `帮我生成一个相亲交友资料页`
- `把这些信息做成分享链接`

### 8.2 管理

- `skill 简历项目 帮我把这个资料页下线`
- `skill 简历项目 帮我重新上线这个页面`
- `skill 简历项目 帮我重置这个页面的聊天次数`
- `skill 简历项目 根据这个管理链接重新生成资料`
- `帮我把这个资料页下线`
- `帮我重新上线这个页面`
- `帮我重置这个页面的聊天次数`
- `根据这个管理链接重新生成资料`

### 8.3 恢复

- `skill 简历项目 恢复分享链接`
- `skill 简历项目 重新恢复固定入口`
- `skill 简历项目 让所有页面重新可以访问`
- `恢复分享链接`
- `重新恢复固定入口`
- `让所有页面重新可以访问`

---

## 9. 关键文件

- [CLAW_USAGE.md](./CLAW_USAGE.md)
- [SKILL.md](/path/to/codex/skills/profile-share-assistant/SKILL.md)
- [publish_profile.js](/path/to/codex/skills/profile-share-assistant/scripts/publish_profile.js)
- [manage_profile.js](/path/to/codex/skills/profile-share-assistant/scripts/manage_profile.js)
- [restore_fixed_entry.js](/path/to/codex/skills/profile-share-assistant/scripts/restore_fixed_entry.js)

---

## 10. 一句话版本

你后面最常用的，其实就这几句：

- `skill 简历项目 帮我生成一个简历页，信息如下：...`
- `skill 简历项目 帮我生成一个相亲交友资料页，信息如下：...`
- `skill 简历项目 根据这个管理链接把页面下线：...`
- `skill 简历项目 根据这个管理链接重置次数：...`
- `skill 简历项目 根据这个管理链接重新生成资料：...`
- `skill 简历项目 恢复分享链接`
- `帮我生成一个简历页，信息如下：...`
- `帮我生成一个相亲交友资料页，信息如下：...`
- `根据这个管理链接把页面下线：...`
- `根据这个管理链接重置次数：...`
- `根据这个管理链接重新生成资料：...`
- `恢复分享链接`
